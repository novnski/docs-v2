---
title: "Migrating from the Moralis REST API"
description: "Already using Moralis REST API endpoints? This is the background you need before you move an endpoint onto Data Feeds: how the two differ, why the schema is not a copy, and the gotchas to plan for."
---

If you already call the Moralis Web3 Data API, you can move endpoint by endpoint onto **Data Feeds**.
You go from a pre-packaged REST response to the **underlying decoded data that produces it**, served from a
database you own, in real time, with no per-call limits and full control over how you store and query it.

This page is the background to read **once**. When you are ready to move a specific endpoint, go to the
[endpoint list](/data-feeds/migration/endpoints) and pick the one you call today.

## API vs. Data Feeds: why they are different

The Data API hands you a **finished answer**. A single call returns a wallet's tokens, already joined to
prices, metadata, and spam flags, shaped into one JSON response.

Data Feeds hand you the **decoded onchain data that answer is built from**, streamed into a database you own.
You assemble the answer yourself, which is what gives you no per-call limits, full history, and control over
filtering and pricing.

That trade has one consequence worth internalizing before you start:

<Note>
**A Data Feed reconstructs an endpoint, it is not a byte-for-byte copy of the old response.** The same facts
are there, but they arrive as raw rows across a few datasets rather than one pre-joined object. You do the
join (usually one SQL view), and you decide what to do about the off-chain extras.
</Note>

## Start with the model, once

Two concept pages explain how Data Feeds work, so every guide links here instead of repeating it. Read these
first and the individual guides go quickly:

- [What are Data Feeds?](/data-feeds/concepts/what-are-data-feeds) The one data lake, and the ways to consume it (your
  Kafka / AMQP / SQS clients, REST / Arrow Flight, the sink into your own database, or query engines via
  Iceberg).
- [Data Feeds vs. the legacy API](/data-feeds/concepts/data-feeds-vs-the-legacy-api) The fidelity model: a Data Feed
  reconstructs an endpoint from raw onchain data, it is not a byte-for-byte copy of the old response.

## The schema gotchas to plan for

These are the differences that surprise people first. Each migration guide states the answer for its own
endpoint, but the pattern is always one of these:

| You had in the API response | Where it comes from in Data Feeds |
|---|---|
| **One pre-joined object** (e.g. a wallet token with balance, price, metadata) | Several datasets you join yourself, usually in one SQL view. One endpoint often maps to **more than one recipe**. |
| **A derived USD value** (price, net worth, USD-at-risk) | **Calculated** from live DEX trades. Very close to the old value, best compared with a small tolerance, not byte-equality. |
| **An off-chain signal** (logos, spam flags, security scores) | **Not in the lake.** Bring your own source, or drop the field. |
| **A field that needs full history to be correct** (balances, holders, approvals) | Correct only once you backfill. Decide your [backfill](/data-feeds/concepts/history-and-backfill) before you start. |

### How to read the fidelity of a migration

Every guide classifies each output value so you can plan with confidence. Throughout, you will see three
labels:

| Label | Meaning |
|---|---|
| **exact** | Comes straight from the chain, identical to the old endpoint (balances, transfers, metadata). |
| **calculated** | Derived from real onchain activity, for example USD prices from live DEX trades. Very close to the old value, best compared with a small tolerance. |
| **add yourself** | An off-chain signal (logos, spam flags, security scores) that is not in the lake. Bring your own source, or skip it. |

## Find your endpoint

When you are ready, the **[endpoint list](/data-feeds/migration/endpoints)** maps every covered Moralis REST
endpoint to its migration guide, ordered by usage. The sidebar groups the same guides by the API they belong
to — Wallet, Token, Price, DeFi, NFT, Blockchain, and Solana — so you can also jump straight to the one you
call today.

New to Data Feeds entirely? Start with the **[quickstart](/data-feeds/migration/quickstart)** to get live data
flowing into your own database in about ten minutes, then come back and migrate your endpoints.

## Not yet covered

These are intentionally out of this set for now:

- **Solana endpoints** are parked while the Solana recipe and replica story is confirmed. The data exists on
  the Solana feed, but the recipes are not shipped yet.
- **Search and discovery endpoints** (`/tokens/search`, `/discovery/*`) are a search product rather than an
  indexed feed, so they are out of scope for a data-lake migration.
- **RPC node methods** (`eth_*`) belong to the node product, not Data Feeds.

## How each guide is structured

Every guide follows the same shape, so once you have read one you can skim the rest:

1. **How Data Feeds works**, and the best-fit way to consume this endpoint's data.
2. **What to expect**, your outputs grouped as comes-through-exactly, calculated-for-you, yours-to-control,
   and add-the-extras.
3. **Before you begin**, prerequisites and the backfill decision for this endpoint.
4. **Build it with the sink**, turn on the recipe(s), join them in a view, query from your app.
5. **Other ways to consume this data**, the same feed via Kafka / AMQP / SQS, REST / Arrow Flight, or Iceberg.
6. **Field mapping reference**, the per-field table with the fidelity label.
7. **Good to know**, the gotchas worth knowing before you ship.
