---
title: "What Are Data Feeds?"
description: "Moralis Data Feeds deliver the decoded onchain dataset as a real-time stream available inside your own stack, consumed through whatever already fits it."
---

Moralis **Data Feeds** give you the **decoded onchain dataset**, every block turned into balances,
transfers, swaps, prices, approvals, NFT activity, and raw logs, as a **real-time stream available inside
your own stack**. It's the same data that powers Moralis, available for you to consume directly, without
building the blockchain indexing layer behind it.

## One data lake, many ways out

Data Feeds is a single service over one data lake. Moralis writes each chain's data **once**, then exposes that
same data through whatever interface fits your stack, so you adopt it **without re-tooling**:

### Stream it with the tools you already use
Consume feeds through **supported streaming interfaces**, using the client libraries your team already runs,
depending on the feed and current availability:

- **Kafka-compatible** clients.
- **RabbitMQ / AMQP** clients.
- **SQS-compatible** clients.

…or read directly over **REST / HTTP** or **Arrow Flight (gRPC)** when you want high-throughput columnar
pulls. See [Streaming](../access-methods/streaming) and [Direct reads](../access-methods/direct-reads).

### Land it with the sink
Point a **template** at your own **Postgres, MySQL, or ClickHouse** and the sink continuously lands
ready-to-query tables. Moralis does the heavy lifting in the template, decoding, enrichment, and reorg
correction, so you get a production-grade dataset inside your own stack, queried however you like. This is
the smoothest path for request/response needs (e.g. "a wallet's balances"). See
[Database sinks](./destinations).

### Query it from your warehouse
Feed data is designed for **Apache Iceberg**-compatible access, committed as data lands, so your warehouse
and notebooks can read it **without moving data first**, on platforms like **Snowflake, BigQuery, Spark,
Trino, Databricks, DuckDB, Pandas, and dbt**. See
[Warehouse access](../access-methods/warehouse-access) for details and current availability.

> It's the **same data lake** under all of these, they're access modes of one service, not separate products.
> Choose by your stack and use case; you're never locked into one shape.

## What's in a feed

Each record is a **block**, carrying the decoded onchain activity for that block as typed arrays, token
transfers, swaps, price updates, balances, NFT transfers, approvals, and the raw block/transaction/log data.
See [The `block` array](./the-block-array) for how transactions and logs are laid out.

A few properties worth knowing:

- **Decoded, not raw RPC.** Swaps, transfers, balances, and prices are already extracted, you don't decode
  logs yourself (though the raw logs are there if you want them).
- **Real-time and historical.** Read the latest blocks as they land, backfill history, or both.
- **Reorg-aware.** Each block records its position and parent, so consumers (and the sink's templates) correct
  for chain reorganizations rather than serving stale data.
- **Onchain data.** A feed contains what's on the chain. Off-chain signals (token logos, spam/verification
  labels, security scores) are layered on separately, see
  [Data Feeds vs. the REST API](./data-feeds-vs-the-rest-api).

## Where to go next

- **Migrating from a Moralis REST endpoint?** Start with the relevant migration guide.
- **Building something new?** Browse the template cookbook for a ready-to-run feed.
- **Choosing how to consume it?** The [access methods overview](../access-methods/overview) compares all four
  paths with a decision table.
