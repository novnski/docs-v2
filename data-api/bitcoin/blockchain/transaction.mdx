---
title: "Get Transaction"
sidebarTitle: "Get Transaction"
openapi: "/openapi-files/data-api/api-v1.json GET /v1/chains/{chainAlias}/transactions/{txHash}"
---

import { EndpointMeta } from '/snippets/endpoint-meta.jsx'

<EndpointMeta cus={100} />

<Note>
  For Bitcoin, pass `bitcoin` (or `bitcoin-mainnet`) as `chainAlias` and the Bitcoin txid as `txHash`.
</Note>
