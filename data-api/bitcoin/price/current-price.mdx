---
title: "Current Price"
sidebarTitle: "Current Price"
openapi: "/openapi-files/data-api/bitcoin-api-v1.json GET /v1/chains/{chainAlias}/tokens/{tokenAliasOrTokenAddress}/price"
---

import { EndpointMeta } from '/snippets/endpoint-meta.jsx'

<EndpointMeta cus={10} />

<Warning>
  **Bitcoin** is fully live on this endpoint. **EVM and Solana** support is in early-access beta and launching soon — the chain parameters may work today, but production usage isn't recommended yet.
</Warning>

<Note>
  For BTC, pass `bitcoin` as `chainAlias` and `native` as `tokenAliasOrTokenAddress`.
</Note>
