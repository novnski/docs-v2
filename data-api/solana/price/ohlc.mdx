---
title: "OHLC by Pair Address"
sidebarTitle: "OHLC"
openapi: "/openapi-files/data-api/solana-api.json GET /token/{network}/pairs/{address}/ohlcv"
---

import { EndpointMeta } from '/snippets/endpoint-meta.jsx'

<EndpointMeta cus={150} />

<Warning>
This endpoint requires a **pair address**, not a token address. To find the pair addresses for a token, use the [Token Pairs endpoint](/data-api/solana/token/pairs/token-pairs).
</Warning>
